export default function Products(){
    return(
        <div>
            <h3>Automate data entry with Invoice OCR</h3>
            <p>
            Real-time data capture from unstructured invoices, bills & receipts down to SKU level line-items, into standardized JSON.
            No template - setup required.</p>
        </div>
    );
}